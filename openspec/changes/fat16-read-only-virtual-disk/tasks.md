## 1. FAT16 骨架与静态布局

- [x] 1.1 确定第一阶段 FAT16 常量参数，包括 sector 大小、cluster 大小、FAT 扇区数、根目录项数量和数据区起始 LBA
- [x] 1.2 定义第一阶段固定目录树，仅包含根目录、`ROM/`、`RAM/` 以及本阶段需要暴露的只读文件与目录项
- [x] 1.3 建立静态 `cluster -> file view` 与 `LBA -> cluster` 映射表，并为未来阶段预留未使用区间
- [x] 1.4 生成 Boot Sector、FAT1、FAT2 和 Root Directory 的只读扇区内容

## 2. MSC 读路径与虚拟磁盘分发

- [x] 2.1 增加最小 `USB MSC` 读路径骨架，支持主机读取扇区数据
- [x] 2.2 实现 `virtual_disk` 只读路由，将主机读请求分发到 Boot/FAT/目录/数据区处理器
- [x] 2.3 为固定目录和小文本文件实现统一的 directory-view / file-view 读处理器
- [x] 2.4 为只读文件和目录项定义稳定的只读语义，确保写请求被拒绝或不改变设备状态

## 3. Cartridge 读取服务抽象

- [x] 3.1 从现有固件逻辑中抽取 ROM 只读服务接口，支持按 offset/length 读取 ROM 内容
- [x] 3.2 从现有固件逻辑中抽取 Save 只读服务接口，支持按 offset/length 读取存档内容
- [x] 3.3 提供 ROM 与 Save 的文件大小来源接口，第一阶段先使用保守固定值并标出后续替换点
- [x] 3.4 确保文件视图层只依赖读取服务抽象，而不直接依赖 CDC 协议命令处理逻辑

## 4. 只读虚拟文件视图

- [x] 4.1 实现 `INFO.TXT` 的动态文本生成
- [x] 4.2 实现 `STATUS.TXT` 的动态文本生成
- [x] 4.3 实现 `/ROM/CURRENT.GBA` 的只读文件窗口，将文件 offset 映射到 ROM 读取地址
- [x] 4.4 实现 `/RAM/CURRENT.SAV` 的只读文件窗口，将文件 offset 映射到 Save 读取地址
- [x] 4.5 为本阶段参数候选项文件提供只读展示内容，验证目录与文件视图扩展机制可用

## 5. 验证与收尾

- [ ] 5.1 在目标主机系统上验证设备可稳定枚举为可读 FAT16 可移动磁盘
- [ ] 5.2 验证根目录与 `ROM/`、`RAM/` 目录结构显示正确且跨重连稳定
- [ ] 5.3 验证 `INFO.TXT`、`STATUS.TXT`、`/ROM/CURRENT.GBA`、`/RAM/CURRENT.SAV` 的只读行为和内容正确性
- [ ] 5.4 记录只读导出吞吐、主机预读行为与兼容性观察，为下一阶段设计提供输入
